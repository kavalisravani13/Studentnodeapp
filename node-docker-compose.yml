version: '3.9'

services:
  studentapp:
    build: .
    ports:
      - "8081:8000"   # Host 8081 → Container 8000
    volumes:
      - .:/app        # Optional: allows live code changes without rebuilding
    restart: unless-stopped
